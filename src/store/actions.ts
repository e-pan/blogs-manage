import * as types from './types'
// import { authQry  } from './../request/apis'

export default {
  // 获取用户权限相关
  // async ['getUserRights']({
  //   commit
  // }, params = {}) {
  //   const res = await authQry(params);
  //   commit(types.USER_RIGHTS, res);
  //   return res || {};
  // }
}
